name: Build Api Docker Image

on:
  push:
    branches: [master]
    paths:
      - backend/Submarine.Api/**
      - backend/Submarine.Core/**
      - .github/workflows/build-master.yml

jobs:
  docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source code
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get short hash of commit
        id: sha_short
        shell: bash
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Submarine.Api/Dockerfile
          push: true
          tags: |
            ghcr.io/simplymedia/submarine-api:${{ steps.sha_short.outputs.sha_short }}
            ghcr.io/project-nzb/submarine-api:latest

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
